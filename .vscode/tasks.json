{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "type": "shell",
            "command": "nim c --cpu:i386 --os:linux --compileOnly --genScript --noMain i_video.nim",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}